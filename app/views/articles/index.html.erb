<div>
  <h1>Bienvenue, ici sont regroupés les différents blogs concernant les wars</h1>
</div>
<div>

<ul>
  <% @articles.each do |article| %>
   <% unless article.archived? %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
  <% end %>
</ul>

<%= link_to "Créer un nouveau blog", new_article_path %>

<%= content_tag(:div, "test", data: {
  controller: "counter"
}) do %>
<%= content_tag(:p, "Tu n'as pas encore cliqué JS", data: {
  counter_target: "output"
  }) %>
<%= content_tag(:button, "Clique ici JS :)", data: {
  action: "click->counter#increment"
}) %>

<% end %>
</div>